/*const uses = ["Journalists", "Bloggers", "Podcasters", "Lecturers", "Interviewers", "Sermoners", "Investigators", "Laywers"];

let useIndex = 0;

function incrementUse(n) {
    useIndex += n;
    if(useIndex >= uses.length) {
        useIndex = 0;
    } else if(useIndex < 0) {
        useIndex = uses.length - 1;
    }
    let use = document.getElementsByClassName("use");
    use[0].innerHTML = uses[useIndex];
}*/